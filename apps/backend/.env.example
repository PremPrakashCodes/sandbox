# Application settings
APP_NAME="Sandbox API"
VERSION="0.1.0"
DEBUG=True

# Server settings
HOST=0.0.0.0
PORT=8000

# CORS settings (comma-separated for multiple origins)
CORS_ORIGINS=["http://localhost:3000"]

# Database settings (uncomment when needed)
# DATABASE_URL=postgresql://user:password@localhost/sandbox